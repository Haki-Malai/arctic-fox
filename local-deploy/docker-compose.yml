services:
  backend:
    build:
      context: ../
      dockerfile: local-deploy/Dockerfile.backend
    image: backend
  frontend:
    build:
      context: ../
      dockerfile: local-deploy/Dockerfile.frontend
    image: frontend
    ports:
      - "3000:80"